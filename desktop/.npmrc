# Route the Electron binary download through a mirror so GitHub's release CDN
# is not a hard dependency (it can be flaky/blocked in some networks). Electron's
# install script reads this through the npm_config_electron_mirror env var.
electron_mirror=https://npmmirror.com/mirrors/electron/
